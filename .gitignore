# =============================================================================
# Whitelist-based .gitignore for dotfiles repository
# =============================================================================
#
# This repository uses a "whitelist" approach:
#   1. Ignore everything by default
#   2. Explicitly allow only the files/directories we want to track
#
# Why this approach?
#   The home/ directory mirrors $HOME structure, which could accidentally
#   pick up sensitive files (credentials, keys), application caches, or
#   other unwanted data. By ignoring everything first, we ensure only
#   intentionally selected configuration files are committed.
#
# How to add new files:
#   - For files in an already-whitelisted directory: just add the file
#   - For a new directory: add `!/home/newdir/**`
#   - For a new individual file: add `!/home/newfile`
#
# =============================================================================

# Ignore everything
*

# Allow directory traversal (required for nested whitelist patterns to work)
!*/

# Whitelist directories (all contents tracked)
!/home/aqua/**
!/home/claude/**
!/home/config/**
!/home/ssh/**
!/zsh/**

# Whitelist individual files in home/
!/home/gitconfig
!/home/gitignore_global
!/home/ideavimrc
!/home/zshenv
!/home/zshrc
!/home/zimrc

# Whitelist root files
!/.gitignore
!/.gitkeep
!/bootstrap.sh
!/Makefile
!/LICENSE
!/README.md
