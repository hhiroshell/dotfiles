name: rust

command: rustup

version:
  policy: latest

# cf. https://rustup.rs/
install:
  - type: brew
    package: rustup
    os: macos

  - type: custom
    os: linux
    check: command -v rustup
    script: |
      curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
